(function(global){"use strict";
var lib = (typeof module !== "undefined" && module.exports) ? require("./_lib.js") : {fmtCurrency:fmtCurrency,fmtNumber:fmtNumber,isSafe:isSafe};
function payment(p, rate, months) { var i=rate/100/12; if(i===0) return p/months; var f=Math.pow(1+i,months); return p*i*f/(f-1); }
function compute(v){var cur=payment(v.currentBalance,v.currentRate,v.remainingYears*12), neu=payment(v.currentBalance,v.newRate,v.newTermYears*12), save=cur-neu;if(save<=0)return {error:"The new payment is not lower than the current payment, so this simple break-even calculation does not apply."};var months=v.closingCosts/save;return {rows:[{label:"Current payment",value:lib.fmtCurrency(cur),rawValue:cur},{label:"New estimated payment",value:lib.fmtCurrency(neu),rawValue:neu},{label:"Monthly savings",value:lib.fmtCurrency(save),rawValue:save},{label:"Break-even",value:lib.fmtNumber(months)+" months",rawValue:months,isTotal:true}],note:"A longer new term can reduce the payment while increasing total interest."};}
if(typeof module!=="undefined"&&module.exports){module.exports=compute;}else{global.CalcCompute=compute;}})(typeof window!=="undefined"?window:globalThis);
