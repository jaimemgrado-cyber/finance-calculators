(function(global){"use strict";
var lib = (typeof module !== "undefined" && module.exports) ? require("./_lib.js") : {fmtCurrency:fmtCurrency,fmtNumber:fmtNumber,isSafe:isSafe};
function payment(p, rate, months) { var i=rate/100/12; if(i===0) return p/months; var f=Math.pow(1+i,months); return p*i*f/(f-1); }
function compute(v){var p=v.loanAmount, old=payment(p,v.interestRate,v.termYears*12), neu=payment(p,Math.max(0,v.interestRate-v.rateReduction),v.termYears*12), cost=p*v.points/100, save=old-neu; if(save<=0)return {error:"The rate reduction must create a positive monthly saving."}; var months=cost/save; return {rows:[{label:"Points cost",value:lib.fmtCurrency(cost),rawValue:cost},{label:"Monthly payment before points",value:lib.fmtCurrency(old),rawValue:old},{label:"Monthly payment after points",value:lib.fmtCurrency(neu),rawValue:neu},{label:"Monthly savings",value:lib.fmtCurrency(save),rawValue:save},{label:"Break-even time",value:lib.fmtNumber(months)+" months",rawValue:months,isTotal:true}],note:"Break-even is a simple estimate based on payment savings."};}
if(typeof module!=="undefined"&&module.exports){module.exports=compute;}else{global.CalcCompute=compute;}})(typeof window!=="undefined"?window:globalThis);
